# 安全策略

## 报告漏洞

请不要在公开 issue 里报告安全漏洞。通过以下任一私有渠道联系维护者，并在标题写明「安全」：

- **GitHub 私有漏洞报告**（推荐）：仓库首页 → Security → **Report a vulnerability**。该入口需仓库管理员在
  Settings → Code security and analysis 里开启「Private vulnerability reporting」；开启后可直接提交并走草稿协同披露。
- 上面无可用入口时，开一个**不含漏洞细节**的 issue（标题写「安全」，正文只说现象与联系方式意向），
  由维护者提供私密渠道后再展开细节。

### 报告里请尽量包含

1. 受影响版本与平台（CLI：`freedom version`，该子命令同时检测最新版本；框架：`node -p "require('./freedom-cli/package.json').version"` 与 `git rev-parse --short HEAD`）
2. 可复现步骤或 PoC（最小即可；不要针对公共服务的实测流量）
3. 影响面判断与可能的修复方向

### 处理节奏

| 阶段 | 目标时限 | 说明 |
|------|---------|------|
| 确认收到 | 3 个工作日 | 回复受理编号与下一步 |
| 评估定级 | 10 个工作日 | 给出严重度与是否需发布通告 |
| 修复发布 | 视严重度 | 高危优先出补丁版本；同时更新本文件下方的已披露条目 |

时限是承诺而非 SLA（当前为单人维护项目），但**不会静默**：到期无进展会主动同步状态。

## 支持哪些版本

| 版本 | 支持 | 说明 |
|------|------|------|
| `1.14.x` | ✅ | 当前发布线，安全修复落在这里 |
| `1.13.x` | ❌ | 不再回补；1.14.0 的 FRDM3 断代后请用新 CLI 重新 `freedom build`（见下） |
| `< 1.13` | ❌ | 不再回补；请先升级 |

## 本项目的安全边界（务必先读）

明确写出「防什么、不防什么」，避免按安全产品标准误判：

- **本仓库闭源专有，但「源码可读」不是漏洞**。为便于审计与自行部署，仓库与 npm 包内的源码、
  注释、模板保持可读；受限的是再分发与衍生（见 [LICENSE](LICENSE)）。因此「源码是公开的」
  「模板里有框架 Go 代码」这类报告不在范围内；请报告的是**能被用来保护他人产物的缺陷**。
- **加密容器（FRDM3）与两档强度——先认清你在哪一档**（`security.go` / `freedom-cli/lib/security.js` /
  `anti_debug_*.go`）。它保护的是**分发产物里的源码与资源可读性**：载荷封进 `resources/app.bin`
  （构建期随机盐 + PBKDF2-HMAC-SHA256 60 万次派生 KEK + AES-256-CTR + Encrypt-then-MAC），
  `resources/.integrity` 是由**发布方 ed25519 私钥签名**的清单，绑定 `app.bin` 哈希、应用标识与容器盐。
  它是**提高逆向成本的尽力而为**，不是可信执行环境：解密后的页面与配置必须交给 WebView，那份明文必然常驻进程内存；
  面对能读进程内存或注入 DLL 的对手不成立。**1.14.0 起收口的一部分**：PBKDF2 派生出的容器密钥不再跨调用驻留
  （只在解密的作用域内存在、返回即抹零），一次内存取样不再等于拿到"可对磁盘产物永久离线解密的钥匙"；
  仍成立的是静态面：主密钥的装配码随壳分发，公开源里有它的生成器，愿意人工逆向壳的人能拼回它
  （1.14.0 起它是**每个产物一份**的随机装配码，所以这份逆向成果对下一个产物无效——抬的是自动化与
  批量脱壳的成本，不是"逆向就是拿不到"，本项目按此如实定级）。
  | 档 | 壳怎么来 | 验签公钥在哪 | 能开 `security: 'high'` 吗 | 挡住谁 |
  |---|---|---|---|---|
  | **Tier B** | 应用自编译壳（`freedom build --security high` 自动现编，或 `freedom shell build` / `cmd/freedom build` / `freedom dev`） | 构建期经 `-X` 注入进该应用的壳 | ✅ 唯一可用档位 | 没有发布方私钥就签不出合法清单 ⇒ 改 `resources/` 重打包、换后端、exe 改名一律**拒绝运行（退出码 70）** |
  | **Tier A** | 通用预编译壳（零工具链，npm 线默认下载） | 无编译期锚点（产物内一切可重写，放公钥等于自证） | ❌ 结构上不具备解密能力，请留在 `basic` | —— |
  **诚实边界**：Tier B 也挡不住"攻击者自己换一对密钥、再编一份壳"——那已经是"整个应用都属于攻击者"，
  再往上一层只有 OS 级出版商信任（Authenticode，本项目缺证书故未强制）。
- **1.14.0 之前的 high 比现在弱，历史产物请按此评估**：1.13.x 的 FRDM2 用**随 npm 包分发**的对称主密钥
  （`lib/security.js` 的 `MASTER_KEY_CIPHER` / `decryptApp` / `buildIntegrity`），且派生输入（应用标识、
  容器盐）都在产物自己身上——因此持有该 CLI 者无需逆向即可离线解密任意 high 产物，并能为篡改后的内容
  生成合法 `.integrity` 让壳照跑（2026-09-25 实测，登记于 `.liangzu/bugs.json` B-20260925-054，1.14.0 关闭）。
  FRDM3 的两处结构变更即为封堵：主密钥改为**每产物随机**且永不进 npm 包，清单改为**发布方私钥签名**。
  旧代际容器（`FRDM1`/`FRDM2`）在新壳上明确拒绝，绝不静默降级。
- **反调试是可选信号**，不是防线。误报环境（自动化测试、CI、远程桌面、部分虚拟化）可用
  `Config.DisableAntiDebug` 或 `FREEDOM_DISABLE_ANTIDEBUG=1` 关闭；关闭不影响容器解密与完整性校验。
- **前端与壳之间不是安全边界**。`window.__freedom_bridge` 对页面内任意脚本可见；
  真正需要收口的能力请用 `Config.Capabilities{Allow,Deny}` 声明式白名单（`capability.go`），
  以及 `sysint_*.go` 里的 shell.open / URL Scheme 白名单。
- **不要给不受信任的页面加载入口**。渲染层复用系统 WebView，等于把浏览器同源能力交给页面；
  加载第三方内容时按 Web 应用的安全模型设计，而不是把它当本地进程。
- **IPC 协议（NDJSON over stdio）无认证**，只用于父进程自己启动的子进程；
  不要把 `FREEDOM_IPC=stdio` 的后端另起为网络监听。

## 已披露

暂无。后续安全修复随补丁版本发布，条目在此登记（含编号、影响版本区间、修复版本），
版本变更细节以 [CHANGELOG.md](CHANGELOG.md) 为准。

本项目当前**不承诺 LTS 分支**：安全修复只落最新发布线（见上表），历史版本请自行升级。
